package userinterface;

public class LevelNumber extends UserInterface{

    public LevelNumber(String imgSrcName, int x, int y) {
        create(imgSrcName);
        setX(x);
        setY(y);
    }
}
